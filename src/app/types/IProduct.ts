export interface IProduct {
    destinationName: string;
    name: string;
    shortDesc: string;
    discount: number;
    price: number;
    pricePreLabel: string;
    pricePostLabel: string;
    image: string;
}